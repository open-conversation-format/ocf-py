"""Bundled OCF JSON schemas, accessible via importlib.resources.

The schema files in this package are vendored from the OCF spec repo
(https://github.com/open-conversation-format/spec). When the spec
publishes a new schema version, the corresponding file is copied here
and the version is added to ocf.core.schema.SUPPORTED_VERSIONS.
"""
