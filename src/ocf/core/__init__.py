"""Core format-handling primitives — canonical JSON, schema validation,
hashing, strip_to_wire, redaction, bundle I/O.

Everything else in ocf-py (exporters, importers, CLI, conformance) builds
on top of these modules.
"""
